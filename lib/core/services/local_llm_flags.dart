/// On-device local LLM removed from the product — Built-in AI + BYOK cloud only.
const bool kLocalLlmEnabled = false;

/// Kept so legacy UI gates compile; always true while local is removed.
const bool kLocalLlmComingSoon = true;
